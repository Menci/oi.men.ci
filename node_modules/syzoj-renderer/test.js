let md = `
$$\\sin x=1$$

$$\\color{red} The whole sentence is red in LaTeX$$

$$\\renewcommand{\\sin}{\\cos}$$

$$\\sin x=1$$

$$
\\begin{aligned}
x&=1\\\\
y+y+y+y+y&=\\text{cR}\\\\
\\text{crAKWhat}&=IOI
\\end{aligned}
$$

$E \\href{javascript:alert("Einstein says so!")}{=} mc^2$

`;

console.log('<link href="./dist/math.css" rel="stylesheet">');
// console.log('<style>td { border: 1px solid #000; }</style>');

import { markdown } from './dist/index';
import util from 'util';
let cache;
// import Redis from 'redis';
// let redisClient = Redis.createClient();
// cache = {
//   get: util.promisify(redisClient.get).bind(redisClient),
//   set: util.promisify(redisClient.set).bind(redisClient),
// };

markdown(md).then(html => {
  console.log(html)
  // process.exit();
});

markdown(md).then(html => {
  console.log(html)
  // process.exit();
});

// let old = require('../syzoj/libs/markdown');
// old(md, html => console.log(html));

